import { css } from "styled-components";

export const flexCentered = css`
  display: flex;
  align-items: center;
  justify-content: center;
`;

export const innerCenterContainer = css`
  height: 100%;
  border-radius: 10px;
  filter: drop-shadow(4px 4px 10px rgba(0, 0, 0, 0.25));
`;
